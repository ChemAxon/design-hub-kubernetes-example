kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: license-volume-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
  volumeName: license-volume
